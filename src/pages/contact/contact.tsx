import React from "react"
import ReactDOM from "react-dom/client";
import ProjectsPage from "./ContactPage";

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <ProjectsPage />
    </React.StrictMode>,
)
